# Add project specific ProGuard rules here.
# V1 ships with minifyEnabled = false, so this file is currently unused,
# but is kept in place for when release shrinking is turned on later.
-keep class com.shiponstore.customermanagement.data.local.entity.** { *; }

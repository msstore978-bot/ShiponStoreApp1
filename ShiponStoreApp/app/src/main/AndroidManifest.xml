<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        NOTE (V1 scope): INTERNET permission is declared because a future step
        (Sync Engine -> Apps Script API) will need it, and because leaving it
        out now would force a manifest change later. No network code runs in
        V1 - PART 7 requires the app to work fully with data/wifi turned off.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".ShiponStoreApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShiponStore">

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- PART 6 / PART 7: developer/offline database test screen -->
        <activity
            android:name=".ui.test.OfflineTestActivity"
            android:exported="false"
            android:label="@string/offline_test_title" />

    </application>

</manifest>
